name = "hf-proxy"
main = "_worker.js"
compatibility_date = "2025-01-01"

[vars]
RESTRICT_BROWSER_ACCESS = "true"

# 限流绑定：每个客户端 IP 每 10 秒最多 10 次请求（按 Cloudflare 数据中心本地计数）
[[ratelimits]]
name = "RATE_LIMITER"
namespace_id = "1001"
simple = { limit = 10, period = 10 }
